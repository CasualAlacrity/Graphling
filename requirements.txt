# Core
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langgraph>=0.2
langsmith>=0.1
pydantic>=2.7

# LLM provider (swap/add others as needed: langchain-anthropic, langchain-google-genai, etc.)
langchain-openai>=0.2
langchain-ollama>=0.2
langchain-anthropic>=0.2

# Convenience
python-dotenv>=1.0

requests
rapidfuzz
# Metaphone encoding for the phonetic fallback in tools/uexcorp/matching.py — speech
# mishearings are a sound problem, and string distance can't see them.
jellyfish

# Voice (push-to-talk + TTS)
sounddevice==0.5.5
soundfile==0.14.0
openai-whisper==20250625
numpy
pynput==1.8.2
elevenlabs==2.52.0

# Trade route tracker (Postgres storage)
sqlalchemy>=2.0
greenlet>=3.0
asyncpg>=0.29
alembic>=1.13

# Trade route tracker (overlay UI)
PySide6>=6.7
qasync>=0.27

# Ledger server (app/server/) — mediates Postgres access behind Discord OAuth + JWT.
# Not needed to run the desktop client itself, only the server (see server/README or
# scripts/mac/run-server.sh), but kept in one requirements.txt rather than a second file
# since both run from the same repo/venv in local dev.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic-settings>=2.5
python-jose[cryptography]>=3.3

# Ledger client (ledger_client.py) — the desktop client's HTTP calls to the server above.
httpx>=0.27